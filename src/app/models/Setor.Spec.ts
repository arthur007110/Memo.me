import { Setor } from '../models/Setor';
describe("Testes referentes à Setor.ts.", function(){
  it("Deve verificar se todos os campos estão preenchidos corretamente", function(){
    expect(object.verificarCampos())toBeFalsy();
  });
});
