import { Usuario } from '../models/Usuario';
describe("Testes referentes à Usuario.ts",function(){
  it("Deve testar o método verificarCampos", function(){
    expect(object.verificarCampos())toBeFalsy();
  });
});
